{
  "name": "hybrid-mlm-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Backend for Hybrid MLM Plan — PV binary engine, BV/royalty/funds, EPIN, wallet, rank & session engine (production-ready).",
  "main": "server.js",
  "type": "module",

  "engines": {
    "node": "18.x",
    "npm": "9.x"
  },

  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",

    "seed": "node scripts/seedInitialData.js",
    "migrate": "node scripts/runMigrations.js",

    "lint": "eslint . --ext .js",
    "format": "prettier --write \"**/*.{js,json,md}\"",
    "test": "jest --runInBand"
  },

  "author": "Dev ji",
  "license": "MIT",

  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^7.5.0",
    "morgan": "^1.10.0",
    "node-cron": "^3.0.2",
    "nodemailer": "^6.9.5",
    "pino": "^8.16.0",
    "uuid": "^9.0.0"
  },

  "devDependencies": {
    "eslint": "^8.52.0",
    "jest": "^29.6.1",
    "nodemon": "^3.0.1",
    "prettier": "^3.6.0"
  }
}
