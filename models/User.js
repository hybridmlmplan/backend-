// user doc additions (high-level)
{
  packageCode,        // Sp/Gp/Rp
  rank,               // e.g., 'Sp Star', 'Rp Diamond Star'
  levelCounts: {      // counts for level eligibility
    level1Directs: Number,
    level2Members: Number,
    level3Members: Number,
    // ...
  },
  isRubyOrAbove: Boolean,
  isDiamondOrAbove: Boolean,
  travelFundEligible: Boolean,
  carFundEligible: Boolean,
  houseFundEligible: Boolean
}
